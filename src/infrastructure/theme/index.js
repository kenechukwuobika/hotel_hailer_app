import { colors } from "./colors";
import { fontFamilies, fontSizes, lineHeights, fontWeights } from "./fonts";
import { spaces } from "./spaces";

export const theme = {
    colors,
    fontFamilies,
    fontSizes,
    lineHeights,
    fontWeights,
    spaces
};